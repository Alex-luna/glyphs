## De onde viemos

Sem release anterior. Evolução a partir de template-simple-v2 (README/stack copiados) e sessões de enxugamento no repo local antes do git.

## Onde estamos

**template-simple-v3** — kit Cursor pessoal Luna Labs (infra only, sem `app/`):

- **Rules:** `default`, `template-onboarding` (condicional), `github-workflow`, `convex-module-deploy`, `aprendizados-projeto`, `O Conselho` (opt-in)
- **Skills base:** `caveman`, `writing-prds`; UI opcional via `install-optional-skills.sh`
- **Scripts:** `commit-push.sh`, `release.sh`, `github-auth.sh`
- **Docs:** `FORK.md`, GitHub/Convex, Blueprint opcional, release notes template
- **GitHub:** repo privado template `Alex-luna/template-simple-v3`

## Para onde vamos

- Manter `skills-bundle` local para UI skills
- Usar **Use this template** em novos projetos; seguir `Docs/FORK.md`
- Próximas releases: semver conforme mudanças em rules/scripts (patch/minor)

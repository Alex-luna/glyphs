## De onde viemos

[v0.1.0](https://github.com/Alex-luna/template-simple-v3/releases/tag/v0.1.0) — primeira release do template v3 (infra Cursor, rules, scripts, Docs/FORK).

## Onde estamos

- **README v3** honesto (infra only, sem `app/`)
- **`Docs/FORK.md`** — checklist de fork
- **`default.mdc`** restaurado enxuto; onboarding condicional sem `app/`
- **Commit/push obrigatório** — `default` + `github-workflow` imperativos; agent não pode encerrar com diff pendente
- Plans fora do git; `skill-builder` removido; `SKILLS_SOURCE` padrão no install script
- Release notes v0.1.0 versionadas no repo

## Para onde vamos

- Manter `skills-bundle` local para UI skills
- Usar template em novos projetos; agent sempre commita/pusha ao fim de batch
